//config file for database

const config = {
    db: {
        host: "localhost",
        user: "newuser",
        password: "Hello123",
        database: "realestate",
        connectTimeout: 60000
    },
    listPerPage: 10,
};
module.exports = config;


